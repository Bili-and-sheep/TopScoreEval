{% extends 'base.html.twig' %}

{% block title %}Stream{% endblock %}

{% block body %}
    <h1>Stream</h1>

    <table class="table">
        <thead>
        <tr>
            <th>id</th>
            <th>Jeu</th>
            <th>Date</th>
            <th>URL</th>
            <th>User</th>
        </tr>
        </thead>
        <tbody>
            <tr>
                <td>{{ stream.id }}</td>
                <td>{{ stream.Jeu.name }}</td>

                <td>{{ stream.startdDate ? stream.startdDate|date('Y-m-d H:i') : '' }}</td>
                <td>{{ stream.url}}</td>
                <td>{{ stream.User.pseudo}}</td>
            </tr>
        </tbody>
    </table>

    <a href="{{ path('app_stream_index') }}">back to list</a>

    <a href="{{ path('app_stream_edit', {'id': stream.id}) }}">edit</a>

    {{ include('stream/_delete_form.html.twig') }}
{% endblock %}
