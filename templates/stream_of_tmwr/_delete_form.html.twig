<form method="post" action="{{ path('app_stream_of_t_m_w_r_delete', {'id': stream_of_t_m_w_r.id}) }}" onsubmit="return confirm('Are you sure you want to delete this item?');">
    <input type="hidden" name="_token" value="{{ csrf_token('delete' ~ stream_of_t_m_w_r.id) }}">
    <button class="btn">Delete</button>
</form>
